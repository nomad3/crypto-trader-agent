{
  "name": "crypto-trader-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  },
  "scripts": {
    "start": "echo 'Please replace with your actual start script, e.g., react-scripts start or vite'",
    "build": "echo 'Please replace with your actual build script, e.g., react-scripts build or vite build'",
    "serve": "serve -s build -l 3000"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
