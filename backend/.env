# --- WARNING ---
# This file contains DUMMY placeholder values for local development.
# Replace with your REAL API keys if you intend to connect to actual services.
# DO NOT commit this file with real keys to version control.

BINANCE_API_KEY=DUMMY_BINANCE_KEY_REPLACE_ME
BINANCE_SECRET_KEY=DUMMY_BINANCE_SECRET_REPLACE_ME
GEMINI_API_KEY=DUMMY_GEMINI_KEY_REPLACE_ME

# --- Database ---
# Select DB_TYPE: "sqlite" or "postgres"
DB_TYPE=postgres

# Option 1: Define full URL (overrides individual components if DB_TYPE=postgres)
# DATABASE_URL=postgresql://user:password@db:5432/trading_db
DATABASE_URL=

# Option 2: Define individual components (used if DATABASE_URL is empty and DB_TYPE=postgres)
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=trading_db

# Option 3: For SQLite (used if DB_TYPE=sqlite and DATABASE_URL is empty)
# Default path relative to backend dir: sqlite:///./backend/trading_agents.db
